apiVersion: v1
kind: Service
metadata:
  name: mcc-postgres
  namespace: {{ .Values.namespace | quote }}
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: {{ .Values.postgres.port }}
      targetPort: postgres
  selector:
    app: mcc-postgres